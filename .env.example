# Environment variables for production deployment
# Copy these to your hosting platform's environment variables

PORT=5000
MONGODB_URI=mongodb://localhost:27017/recipeplatform
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_secure_for_recipe_platform_2024
JWT_EXPIRES_IN=7d
NODE_ENV=production

# For MongoDB Atlas (cloud database), replace MONGODB_URI with:
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# Make sure to set NODE_ENV=production in your deployment platform