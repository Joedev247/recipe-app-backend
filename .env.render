# Production Environment Variables for Render
# Copy these values to your Render Dashboard > Environment tab

NODE_ENV=production
PORT=10000
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_secure_for_recipe_platform_2024
JWT_EXPIRES_IN=7d
MONGODB_URI=mongodb+srv://joedev:Joedev247@cluster0.qgqocvp.mongodb.net/recipeplatform?retryWrites=true&w=majority&appName=Cluster0

# Instructions:
# 1. Create MongoDB Atlas account at https://cloud.mongodb.com
# 2. Create a free cluster
# 3. Create database user and get connection string
# 4. Replace the MONGODB_URI above with your actual connection string
# 5. Add these environment variables to Render Dashboard
# 6. Redeploy your service